kind: Pod
metadata:
labels:
    some-label: jenkins-slave
spec:
containers:
- name: docker-slave
  env:
    - name: docker-slave
      value: docker-slave
- name: docker
  image: docker
  command:
      - cat
  tty: true
  env:
    - name: docker-slave2
      value: docker